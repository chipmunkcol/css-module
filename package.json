{
  "name": "@chipmunkcol/css-module",
  "version": "0.1.6",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "build": "webpack --config webpack.config.js",
    "release": "npm version patch && git push --follow-tags && gh release create v$(node -p \"require('./package.json').version\") dist/* --title \"Release v$(node -p \"require('./package.json').version\")\" --notes \"Automated release\""
  },
  "author": "",
  "license": "ISC",
  "publishConfig": {
    "@chipmunkcol:registry": "https://npm.pkg.github.com"
  },
  "dependencies": {
    "@chipmunkcol/css-module": "^0.0.36",
    "sweetalert2": "^11.12.4",
    "webpack-manifest-plugin": "^5.0.0"
  },
  "devDependencies": {
    "compression-webpack-plugin": "^11.1.0",
    "css-loader": "^7.1.2",
    "file-loader": "^6.2.0",
    "style-loader": "^4.0.0",
    "webpack-cli": "^5.1.4"
  }
}
